/** Name of the httpOnly cookie that carries the customer portal session token. */
export const PORTAL_COOKIE = "rpg_portal_session";
export const AGENT_URL = process.env.AGENT_URL ?? "http://localhost:8000";
